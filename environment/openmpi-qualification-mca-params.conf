# Deliberately empty gpmeep release-qualification MCA parameter set.
#
# The fixed benchmark and receipt-producing builder point Open MPI at this
# source-bound file so per-user and mutable environment-prefix mca-params.conf
# files cannot silently alter transport selection or timing.
